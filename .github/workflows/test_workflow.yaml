name: Main project workflow

on:
  pull_request:
    types: [synchronize, opened, reopened, ready_for_review]

jobs:
  cross-repo-pipeline:
    runs-on: ubuntu-latest
    steps:
      - name: Cross Repo Pipeline
        uses: amandastecz/pipeline-trigger@v1.1
        with:
          pat-token: ${{ secrets.Test_PAT }}
          owner: MiguelazoDS
          repo: Test_Info

